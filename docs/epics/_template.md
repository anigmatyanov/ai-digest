---
id: E-XXX
title: ""
status: draft # draft | todo | in-progress | review | done | blocked | dropped
priority: P2 # P1 | P2 | P3
size: S # S (≤ полдня) | M (≤ ~1 день агентной работы). L не бывает.
autonomy: auto # auto | plan-gated | paired
area: "" # connectors | pipeline | llm | telegram | web | profiles | infra | tooling
depends_on: [] # [E-004] — должны быть done
serialize: [] # метки из реестра .claude/rules/backlog.md

costs_llm:
  false # true → эпик меняет промпты/модель/число вызовов.
  # Тогда `pnpm cost:report` обязателен в ## Верификация,
  # а дельта к costs/baseline.json — в отчёте.
touches_output:
  false # true → эпик меняет то, ЧТО читатель увидит в выпуске.
  # Тогда обязателен `pnpm golden` + дифф выпуска в отчёте,
  # и autonomy не может быть auto.
ui_visible: false # true → на /epic-review нужен скриншот страницы с превью-порта.
cloud_safe:
  true # false → нужны живая сеть, реальные секреты или взгляд на превью;
  # облачная routine такой эпик брать не должна.

claimed_by: "" # agent/<slug> — ставит /epic-next; руками не редактировать
branch: "" # agent/<slug> — ставит /epic-next
created: 2026-08-25
done: "" # YYYY-MM-DD — ставит /finish-branch
discovered_from: "" # E-NNN, если эпик родился в ходе исполнения другого
---

# E-XXX: <название>

## Цель

Одно предложение: что станет правдой после мержа. Наблюдаемый результат, не список работ.

## Контекст (читать перед началом, по порядку)

1. `packages/<...>/src/<...>.ts` — точка расширения; образец с тем же решением: `<файл>`
2. [.claude/rules/<...>.md](../../.claude/rules/) — правила для этой области
3. <ссылки, не инлайн-копии: file:line переживает рефакторинг, вставленный сниппет — нет>

## Объём — карта файлов

Менять можно **только** это (плюс соседние `*.test.ts` и файл этого эпика):

- `packages/<...>/src/<...>`
- `fixtures/<connector>/...`

## Вне объёма

- Что осознанно не делаем и куда отложено (ссылка на эпик).

## Критерии приёмки

<!-- «КОГДА <условие>, ТО <наблюдаемый результат>».
     Исполнитель ставит галочку ТОЛЬКО по факту проверки и НИКОГДА
     не редактирует и не переформулирует текст критерия. -->

- [ ] КОГДА …, ТО …
- [ ] КОГДА …, ТО …
- [ ] (для коннектора) КОГДА фикстура подменена на ответ изменившегося формата,
      ТО прогон падает с `SourceDriftError` с указанием поля — а не отдаёт ноль элементов
- [ ] (для `touches_output: true`) КОГДА прогнан `pnpm golden`, ТО структурные ассерты
      зелёные, а дифф выпуска приложен к отчёту
- [ ] (для `costs_llm: true`) КОГДА прогнан `pnpm cost:report`, ТО дельта к baseline
      названа в отчёте, и baseline обновлён в этом же PR

## Верификация

```bash
pnpm verify
pnpm test -- <pattern>
# сборку доказывай артефактом, а не exit-кодом:
pnpm build && pnpm build:proof
```

Ручная проверка на превью (порт слота — в `.claude/worktrees/.slots`):

1. …

## Границы

**Никогда:** публиковать что-либо наружу; править профили других тем; трогать файлы вне карты; обновлять golden-снапшоты без объяснения.

**Спросить заранее:** новая env-переменная, новая зависимость, любая миграция, любое отступление от карты файлов, любое понижение модели.

## План реализации

_Заполняет исполнитель после чтения «Контекста», ДО первого кода, отдельным коммитом. Для `plan-gated` — пишет read-only Plan-агент, владелец одобряет до claim'а._

## Заметки (append-only)

_Хронология: открытия, отклонения от плана, ссылки на стабы `E-XXXa`, дословные предложения правок owner-only секций, Proposal из rules retrospective. Только дописывать в самый конец._
