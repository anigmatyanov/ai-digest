node_modules/
dist/
.next/
coverage/
runs/
pnpm-lock.yaml
fixtures/
golden/

# Prettier reflows a long inline YAML sequence in epic frontmatter into a block one.
# The parser now handles both forms, but formatting must not silently rewrite a contract:
# that is how E-001 lost all nine of its serialize labels in a diff that looked cosmetic.
docs/epics/

# Generated by scripts/gen-connectors.mjs, whose --check compares the file byte for byte.
# Formatting it makes `pnpm format` break `pnpm verify` — measured red on main 2026-08-29.
packages/connectors/src/registry.ts

# Same reason as the eslint ignore: a sibling agent's worktree is not this tree's code.
.claude/worktrees/
